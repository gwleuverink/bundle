providers:
  - Workbench\App\Providers\WorkbenchServiceProvider
  - Leuverink\Bundle\ServiceProvider
  - Livewire\LivewireServiceProvider

workbench:
  start: "/"
  install: false
  discovers:
    web: true
    api: false
    commands: false
    components: true
    views: true
  build: []
  assets: []
  sync:
    # Setup done via composer script instead: composer setup-workbench
